server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true # ???? sql ??
    properties:
      hibernate:
        format_sql: true # sql ??? ??
        default_batch_fetch_size: 500 # ??? ??? ?? ? ?? ???
    open-in-view: false # ??? ????? ?? ??? ???? ??? ??

  servlet:
    multipart:
      max-file-size: 10MB # ?? ??? ?? ? ??? ?? ??? 10MB ??
      max-request-size: 100MB # ?? ??? ?? ? ?? ?? ???? 100MB ??

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  level:
    org.hibernate.type: trace # ???? ??? ????? ? ? ?? ?? ??
    com.app: debug # todo ??? ??

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

kakao:
  client:


token:
  secret: erjgm243ew24jgieow3fkmerg34ger== # ??? ???
  access-token-expiration-time: 900000  # 15? 1000(ms) x 60(s) x 15(m)
  refresh-token-expiration-time: 1209600000 # 2? 1000(ms) x 60 (s) x 60(m) x 24(h) x 14(d)
